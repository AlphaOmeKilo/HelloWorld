box: maven:latest
build:
  steps:
    - script:
        name: Maven Execution
        code: |
          mvn clean install

deploy:
  steps:
    - internal/docker-push:
        username: $USERNAME
        password: $PASSWORD
        tag: latest
        repository: ocmicrodemo/ocmicrodemo
        registry: https://registry.hub.docker.com
